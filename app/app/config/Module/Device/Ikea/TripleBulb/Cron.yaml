---

jobs:
  - interval: 5
    if:
      - ${Properties.selfObjectName}.status is not offline
      - ${Properties.bulb-1}.status is offline
      - ${Properties.bulb-2}.status is offline
      - ${Properties.bulb-3}.status is offline
    set:
      ${Properties.selfObjectName}.status: offline
