#!/bin/ash

if [ "`cat /app/data/.env`" != "prod" ]; then
  exit 0
fi

while [ 1 = 1 ]; do
	python3 /app/modules/broadlink2mqtt/mqtt-listener.py

	echo " > Service stopped, restarting in 5 seconds..."
	sleep 5s
done
